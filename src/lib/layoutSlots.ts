/**
 * 레이아웃이 페이지에 빌려주는 자리(slot)의 DOM id.
 *
 * 페이지 컴포넌트가 레이아웃 영역에 무언가를 그려야 할 때 쓴다. 레이아웃이
 * 기능 컴포넌트를 직접 import 하면 `layout/` → `room/` 처럼 **기능 간 교차
 * import**(불문율 #7)가 되므로, 레이아웃은 빈 자리만 내주고 내용은 페이지가
 * portal 로 채운다. 의존 방향이 페이지 → 레이아웃 한쪽으로만 흐른다.
 */

/** 우측 사이드바(RNB) 본문. 토론방이 현재 주제 패널을 여기에 그린다. */
export const RNB_SLOT_ID = "rnb-slot";
