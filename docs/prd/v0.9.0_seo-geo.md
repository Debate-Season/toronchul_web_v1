# 검색 v0.9.0 — SEO / GEO 기반 구축

> 선행 참고: `docs/ideation/seo-strategy.md`, `docs/ideation/seo-implementation.md`, `docs/ideation/SKILL.md`
> (모두 **다른 프로젝트(BigValue)** 의 산출물이다. 규모·도메인이 달라 그대로 쓰지 않는다 — §3.3)
>
> 이 문서는 **무엇을 넣을지**와 **왜 그렇게 정했는지**를 확정한다.
> 실행 절차와 코딩 규칙은 `.claude/skills/seo-geo/SKILL.md` 와 그 `references/` 가 담당한다.

**개정 이력**

| 날짜 | 내용 |
|---|---|
| 2026-08-20 | 최초 작성 |
| 2026-09-01 | `docs/ideation/seo-geo-skill-research.md`(24개 소스) 반영. **§3.3 GEO 3축·AI 봇 3계층 인용 철회**, **§5.4 AI 뉴스 근거를 블로그→Google 스팸정책 원문으로 교체 + 발행 게이트화**, §6.2 `generateSitemaps` 시그니처 고정, **§6.3 AI 봇 차단을 GEO 레버에서 라이선스 결정으로 재분류 + Yeti [P0]**, §7-9·7-10·7-11 신설, §9 검증에 audit.sh·Naver 항목 추가 |

---

## 1. 배경 — 지금 우리는 검색에 존재하지 않는다

측정부터 했다. 2026-08-20, Googlebot User-Agent 로 실서비스 이슈 상세를 요청한 결과다.

```bash
curl -A "Mozilla/5.0 (compatible; Googlebot/2.1; +http://www.google.com/bot.html)" \
     https://toronchul.app/issue/8/도널드-트럼프-2기-행정부
```

| 항목 | 실측값 |
|---|---|
| HTTP 상태 | 200 |
| 응답 크기 | 17,645 bytes |
| **스크립트·스타일 제거 후 본문 텍스트** | **30자** — `"토론철 토론철 홈 이슈맵 앱 다운로드 실시간 핫한 토론"` |
| `<title>` | `토론철` (전 페이지 동일) |
| `<meta name="description">` | `토론철 - 토론 플랫폼` (전 페이지 동일) |
| `<link rel="canonical">` | 없음 |
| JSON-LD | 없음 |
| `/robots.txt` | **404** |
| `/sitemap.xml` | **404** |

17KB 를 받아서 30자를 읽는다. 그 30자는 전역 네비게이션이고, 이슈 제목조차 들어 있지 않다.

원인은 단순하다. **색인 대상 4개 라우트가 전부 `"use client"`** 다.

```
src/app/issue/[id]/page.tsx                                  "use client"
src/app/issue/[id]/[slug]/page.tsx                           "use client"
src/app/issue/[id]/[slug]/[threadId]/[threadSlug]/page.tsx   "use client"
src/app/page.tsx                                             "use client"
src/app/map/page.tsx                                         "use client"
```

`src/lib/seo.ts` · `src/app/robots.ts` · `src/app/sitemap.ts` 는 **존재하지 않는다.**

> **2026-09-01 재측정 — 본문 30자, robots·sitemap 404, `/issue/8`·`/issue/8/틀린슬러그`·`/issue/abc/x` 전부 200. 위 표에서 바뀐 것이 없다.**
>
> 그 사이 `layout.tsx` 에 OG·twitter 메타는 들어갔다(응답 17,645 → 23,388 bytes). 다만 `title.template`·`robots`·Organization JSON-LD 는 아직 없고, 도메인이 `const SITE_URL` 로컬 선언으로 하드코딩돼 있다(`layout.tsx:6`) — §6.1 위반이다. **크롤러가 읽는 본문은 30자 그대로다.**

즉 현재 상태는 "SEO 가 약하다" 가 아니라 **"SEO 가 0"** 이다. 이 사실에는 뒤집어 볼 면이 있다 — §3.1.

---

## 2. 목표

토론철의 4개 엔티티가 **각각 독립된 상세 페이지로** 일반 검색(Google·Naver·Bing)과 생성형 AI 검색(ChatGPT Search·Perplexity·Claude·Gemini·AI Overviews)에 잡히게 한다.

| 엔티티 | 페이지의 핵심 콘텐츠 | 권위의 원천 |
|---|---|---|
| **이슈** | 이슈위키(개요·배경) + 사건 타임라인 | 나무위키형 레퍼런스 문서 |
| **사건** | 실시간성(언제 무슨 일이) + 맥락 설명(왜 중요한가) | 최신성 + 해설 |
| **토론** (주제별) | 채팅 내역 기반의 토론 진행 경과 | 1차 사료 — 다른 데 없는 원본 데이터 |
| **AI 뉴스 기사** | 토론철이 생성한 기사 본문 | 자체 발행 콘텐츠 |

레퍼런스 모델은 **나무위키**다. 나무위키가 검색을 지배하는 이유는 백링크가 아니라 구조다 — 하나의 주제에 하나의 URL, 그 URL 이 그 주제에 대해 인터넷에서 가장 긴 글, 목차로 쪼개져 부분 인용이 쉬움, 문서가 계속 갱신됨. 토론철은 여기에 **나무위키가 못 가진 것** 을 더한다: 그 주제에 대해 실제 사람들이 찬반으로 나뉘어 나눈 대화 원문.

> **GEO 관점에서 이게 왜 결정적인가** — LLM 은 "3대 특검이 뭔가"는 나무위키에서 답할 수 있다. 하지만 **"3대 특검에 대한 여론이 어떻게 갈리나"** 는 답할 소스가 없다. 그 질문의 유일한 1차 사료가 되는 것이 토론철의 GEO 전략이다.

---

## 3. 확정 사실

### 3.1 지금은 URL 을 바꿀 수 있는 마지막 시점이다

색인된 페이지가 0개고 사이트맵도 없다. **잃을 링크 권위가 없다.** URL 스킴을 지금 확정하면 마이그레이션 비용이 0 이고, 색인이 시작된 뒤에는 영구적으로 리다이렉트 체인을 짊어진다.

따라서 v0.9.0 은 "메타태그를 붙이는 작업"이 아니라 **URL 스킴 확정 작업** 이 먼저다. (§4)

### 3.2 SSR 에 필요한 API 는 대부분 인증이 필요 없다

Swagger(`https://toronchul.app/prod/v3/api-docs`)와 실 curl 로 확인했다.

| 엔드포인트 | 인증 | 실측 | 용도 |
|---|---|---|---|
| `GET /api/v2/wiki?issue-id=N` | **불필요** | 200 | 이슈위키 본문(Markdown) |
| `GET /api/v2/room?issue-id=N` | **불필요** | 200 | 컨테이너 방 + 스레드 목록 |
| `GET /api/v1/users/home` | **불필요** | 200, 이슈 17건 | 사이트맵 이슈 목록 |
| `GET /api/v1/issue-map` | **불필요** | 200 | 이슈 목록(페이지네이션) |
| `GET /api/v1/issue?issue-id=N` | Swagger 상 JWT | **200 (익명)** | 이슈 상세 + 커뮤니티 분포 |
| `GET /api/v1/chat/rooms/{roomId}/messages` | **JWT 필수** | **401 MISSING_ACCESS_TOKEN** | **채팅 내역 — 토론 페이지 본문** |

**서버 컴포넌트에서 토큰 없이 호출하면 이슈 페이지는 오늘 당장 SSR 이 가능하다.** 반면 토론 페이지의 핵심 콘텐츠인 채팅 내역은 401 이라 **크롤러에게 보여줄 방법이 현재 없다.** → §7-1 백엔드 요청.

> `apiFetch` 는 401 이면 `@/lib/auth/logout` 을 동적 import 한다(브라우저 전용 코드). 다만 그 분기는 `res.status === 401 && token` 조건이라 **토큰 없이 부르는 SSR 경로에서는 타지 않는다.** SSR 호출은 반드시 토큰 없이 한다.

### 3.3 이 프로젝트는 pSEO 가 아니다 — 참고 문서를 그대로 쓰면 안 된다

`docs/ideation/*` 는 700만+ 페이지 규모 프로그래매틱 SEO 프로젝트의 문서다. 토론철의 현재 색인 대상은 다음과 같다.

```
이슈 17개 · 스레드 ~30개 · 정적 페이지 ~3개  →  총 50개 남짓
```

| 참고 문서의 전제 | 토론철에서의 실제 |
|---|---|
| 크롤 버짓 보호가 최우선 | **무관.** 50개 URL 은 Googlebot 이 한 번에 다 돈다 |
| Thin Content 임계값·경고 배너 정책 | **다른 문제다.** 우리는 표본이 작은 게 아니라 **본문이 아예 없다** |
| 사이트맵 40,000 URL 분할 · OOM · lazy 로딩 | 불필요. 단일 `sitemap.xml` 로 충분 |
| AWS WAF Bot Control 연동 | 인프라가 다르다(Vercel). 해당 없음 |
| IndexNow 대량 제출 | 후순위. 갱신 빈도가 낮다 |
| Phase 별 색인율 70% 게이팅 | 무의미. 전량 색인이 기본 기대치 |

**가져올 것은 원칙이고, 버릴 것은 규모 대응이다.** 가져오는 것: canonical 단일 출처 아키텍처, `lastmod` 는 실제 데이터 갱신일만, noindex↔sitemap 동기화 불변, JSON-LD 는 화면 콘텐츠와 일치.

> **GEO 3축(엔티티 명확성·인용 가능성·사실 밀도)과 AI 봇 3계층은 참고 문서에서 그대로 가져오지 않는다.** 2026-08-21 리서치(`docs/ideation/seo-geo-skill-research.md`)에서 근거가 바뀌었다 — GEO 기법은 Princeton GEO 논문의 실측 순위로 대체하고, AI 봇 차단은 GEO 레버가 아니라 라이선스 결정으로 재분류했다. §6.3 및 스킬의 `references/{geo-evidence,crawler-policy}.md` 를 본다.

우리 고유의 문제는 정반대다: **URL 스킴 · 페이지당 콘텐츠 깊이 · 다국어 확장 여지.**

### 3.4 "사건"과 "AI 뉴스 기사"는 아직 백엔드에 없다

Swagger 전수 확인 결과:

- **사건** — 해당 엔드포인트·스키마 없음. `GET /api/v1/lives`·`/api/v1/live/{id}` 는 이름이 비슷하지만 **v0.6.0 의 YouTube 라이브**(`YoutubeLiveDto: id, title, supplier, videoId, category, createAt, src`)다. 사건이 아니다.
- **AI 뉴스 기사** — 없음. `GET /api/v1/home/media` 는 외부 매체 큐레이션(`MediaResponse: supplier, url, src, type, media`)이다. 자체 기사가 아니다.
- **이슈위키** — **있다.** `GET /api/v2/wiki` + 관리자 생성/게시 플로(`/api/v1/admin/wiki/{generate,publish,revisions}`)까지 완비. 다만 실측상 **게시본이 아직 0건**(issue 8·15·20 모두 `status: null, content: null`).

**불문율 #1(필드 조작 금지)에 따라 없는 DTO 를 만들지 않는다.** v0.9.0 은 이슈·토론 2종을 실제로 구현하고, 사건·뉴스는 **URL 자리와 스키마 계약만 예약**한다. (§6)

---

## 4. URL 전략 (이 PRD 의 핵심)

### 4.1 현재 스킴의 문제

`src/lib/slug.ts`:

```ts
export function issueHref(issueId: number, title: string): string {
  return `/issue/${toBase36(issueId)}/${toSlug(title)}`;
}
```

| # | 문제 | 구체 |
|---|---|---|
| 1 | **base36 ID 가 불투명** | 이슈 26 → `/issue/q/...`, 이슈 10 → `/issue/a/...`. 사람도 로그도 읽을 수 없고 Swagger 의 `issueId` 와 대조가 안 된다 |
| 2 | **같은 문서에 URL 이 2개** | `/issue/8` 과 `/issue/8/도널드-트럼프-2기-행정부` 가 **둘 다 200**. 중복 콘텐츠 |
| 3 | **slug 가 정체성에 섞여 있다** | 제목이 바뀌면 URL 이 바뀐다. 잘못된 slug 로 들어와도 그대로 200 → 무한한 중복 URL |
| 4 | **로케일 자리가 없다** | 자동 번역 도입 시 전 URL 을 갈아야 한다 |
| 5 | **`fromBase36` 이 조용히 실패한다** | `parseInt("도널드", 36)` → `NaN`. 404 가 아니라 깨진 렌더 |
| 6 | **사건/뉴스 자리가 없다** | 목표 엔티티 4종 중 2종의 경로가 미정 |

### 4.2 확정 스킴

```
/                                              홈 (ko)
/map                                           이슈맵
/issue/{issueId}-{slug}                        이슈 상세
/issue/{issueId}-{slug}/event/{eventId}-{slug}   사건 상세      ← 예약
/issue/{issueId}-{slug}/debate/{threadId}-{slug} 토론 상세
/news/{articleId}-{slug}                       AI 뉴스 기사    ← 예약

/en/...                                        영어 (동일 구조)  ← 예약
```

실제 예시:

```
/issue/8-도널드-트럼프-2기-행정부
/issue/8-도널드-트럼프-2기-행정부/debate/61-미국은-관세전쟁을-중단해야-하는가
/en/issue/8-the-second-trump-administration
```

### 4.3 설계 근거

**(a) `{id}-{slug}` 융합 — 세그먼트 1개 = 엔티티 1개**

현재의 `{id}/{slug}` 분리형은 `/issue/8` 이라는 "id 만 있는 유효 URL"을 필연적으로 만든다. 융합하면 정식형이 **하나뿐**이다. id 는 항상 선두 숫자이므로 파싱은 `Number(segment.match(/^\d+/)?.[0])` 로 결정적이다. Stack Overflow·Medium 이 쓰는 형태다.

**(b) base36 → 10진수**

- 이슈가 17개인 규모에서 base36 의 길이 이득은 **0자**다.
- Swagger·DB·로그·`curl` 이 전부 10진수다. 변환 계층은 순수 비용이다.
- `fromBase36` 의 조용한 `NaN` 실패가 사라진다.
- `toBase36`/`fromBase36` 은 **제거**하고 `toSlug` 는 유지·보강한다.

**(c) slug 는 장식이다 — 정체성은 id 에만 있다**

| 요청 | 응답 |
|---|---|
| `/issue/8-도널드-트럼프-2기-행정부` | 200 (정식형) |
| `/issue/8-옛날-제목` | **301** → 정식형 |
| `/issue/8` | **301** → 정식형 |
| `/issue/8-` , `/issue/abc-...` | 404 |

id 로 조회 → 현재 제목으로 정식 slug 계산 → 요청 slug 와 다르면 301. 이 규칙 하나가 문제 2·3 을 동시에 없앤다. 제목은 마음대로 바뀌어도 되고, 링크 권위는 항상 정식형 한 곳에 모인다. 나무위키가 리다이렉트로 하는 일을 코드로 한다.

**(d) 로케일은 `as-needed` — 한국어는 접두어 없음**

세 후보를 비교했다.

| 방식 | 채택 | 이유 |
|---|---|---|
| `?lang=en` 쿼리 | ✗ | 크롤러가 별도 페이지로 안 본다. canonical 분리 불가 |
| 전 로케일 접두어 (`/ko/...`, `/en/...`) | ✗ | 대칭적이지만 **지금 당장 전 URL 을 바꾸고 `/` → `/ko` 301 을 영구히 진다.** 홈은 앱스토어·카카오 공유·모바일 앱이 가리키는 최다 피인용 URL 이다 |
| **`as-needed` (ko 무접두, `/en/...`)** | **✓** | **영어 출시 시 한국어 URL 이 단 하나도 바뀌지 않는다.** 리다이렉트 0, 재색인 0 |

`hreflang`: 각 페이지가 `ko`(무접두) ↔ `en`(`/en/…`) 상호 참조, `x-default` 는 한국어를 가리킨다. Next.js `Metadata.alternates.languages` 로 생성한다.

**(e) 로케일 접두어는 색인 대상 트리에만 적용한다 — 인증/앱 경로는 손대지 않는다**

```
로케일 적용:  /  /map  /issue/*  /news/*
로케일 제외:  /login  /oauth/*  /api/*  /settings/*  /profile/*  /onboarding/*
```

`/oauth/callback/kakao` 와 `/api/auth/apple/callback` 은 **카카오·애플 개발자 콘솔에 등록된 Redirect URI** 다. 여기에 로케일 미들웨어가 개입하면 운영 중인 로그인이 즉시 깨지고, 모바일 앱 호환도 무너진다(메모리: 웹 카카오 로그인은 앱과 완벽 호환 필수). 이 경로들은 전부 `noindex` 이므로 SEO 상 손실도 없다.

**(f) slug 생성 규칙 보강**

`toSlug` 에 3가지를 추가한다.

1. **NFC 정규화** — `title.normalize("NFC")`. 한글이 NFD(자모 분리)로 들어오면 같은 제목이 다른 바이트열의 URL 이 되어 301 루프가 난다.
2. **길이 상한 80자** — 단어(하이픈) 경계에서 자른다. 제목이 긴 스레드가 있다.
3. **빈 결과 대비** — slug 가 빈 문자열이면(제목이 특수문자뿐) `-` 없이 id 만으로 정식형을 삼는다.

canonical·사이트맵에 넣는 URL 은 **`encodeURI` 로 퍼센트 인코딩한 형태로 통일**한다. 한 페이지가 원문 URL 과 인코딩 URL 두 형태로 표기되면 중복 신호가 된다.

### 4.4 라우트 파일 변경

| 현재 | 변경 후 |
|---|---|
| `src/app/issue/[id]/page.tsx` | **삭제** (301 로 흡수) |
| `src/app/issue/[id]/[slug]/page.tsx` | `src/app/issue/[issueSlug]/page.tsx` |
| `src/app/issue/[id]/[slug]/[threadId]/[threadSlug]/page.tsx` | `src/app/issue/[issueSlug]/debate/[threadSlug]/page.tsx` |
| — | `src/app/issue/[issueSlug]/event/[eventSlug]/page.tsx` (예약) |
| — | `src/app/news/[articleSlug]/page.tsx` (예약) |

`src/lib/slug.ts` 는 `issueHref`·`threadHref` 시그니처를 유지하되 내부 생성 규칙만 바꾼다. 호출부(`IssueCardNew`, `IssueMapCard`, `IssueThreadList` 등)는 헬퍼만 쓰고 있으므로 수정 범위가 좁다 — **URL 문자열을 직접 조립한 곳이 없는지 검증**이 착수 첫 단계다.

---

## 5. 페이지별 콘텐츠 · 스키마 설계

### 5.1 이슈 상세 — 나무위키형 레퍼런스

**본문 구성(서버 렌더 필수)**

1. H1: 이슈 제목
2. **정의 문단** — 이 이슈가 무엇인지 1~2문장. GEO 인용 가능성의 핵심(§9.2)
3. **이슈위키 본문** — `GET /api/v2/wiki` 의 Markdown. 목차(H2/H3)를 살려 렌더. 부분 인용이 가능해야 한다
4. **사건 타임라인** — 시간 역순 목록 (백엔드 준비 후)
5. **토론 목록** — 스레드별 제목 + 찬반 수. 각각 토론 상세로 링크
6. **참여 커뮤니티 분포** — `IssueDetailResponse.map` (`Record<string, number>`). 구체 수치 = 사실 밀도

> **위키 게시본이 없을 때**(현재 전 이슈) — `content: null` 이면 위키 섹션을 생략하고 토론 목록·타임라인으로 페이지를 구성한다. **`noindex` 를 걸지 않는다.** 참고 문서 §8 의 교훈: 임계값 기반 noindex 는 데이터가 채워질 때마다 색인이 켜졌다 꺼지는 flapping 을 일으킨다.

**JSON-LD**

```
Article
  @id            {SITE_URL}/issue/8-...#article
  headline       이슈 제목
  about          Thing { @id, name, sameAs: [위키백과·나무위키 URL] }   ← GEO 핵심
  datePublished  issue.createdAt
  dateModified   wiki.updatedAt ?? 최신 스레드 활동 시각
  publisher      Organization(토론철)
  mainEntityOfPage / isPartOf
BreadcrumbList   홈 › 이슈 › {제목}
ItemList         사건 타임라인 (ListItem → 사건 상세 URL)
```

> **`about.sameAs` 가 왜 결정적인가** — GEO 3축의 "엔티티 명확성"을 브랜드가 아니라 **주제** 에 적용하는 것이다. `sameAs: ["https://ko.wikipedia.org/wiki/..."]` 는 LLM 에게 "이 페이지의 '3대 특검' 은 네가 이미 아는 그 3대 특검이다" 라고 말한다. 이게 있어야 토론철 페이지가 기존 지식 그래프에 **접속**한다. 외부 URL 은 추측 금지 — 실제 확인된 것만 넣는다.

### 5.2 사건 상세 — 실시간성 + 맥락 (예약)

**본문**: 발생 시각 → 무슨 일이 있었는지(사실) → 왜 중요한지(맥락) → 상위 이슈 링크 → 관련 토론 링크.

**JSON-LD**: `NewsArticle` + `BreadcrumbList` + `isPartOf` → 이슈.

> **`Event` 를 쓰지 않는 이유** — schema.org 의 `Event` 는 "예정된 행사"(공연·컨퍼런스)이고 Google 의 Event 리치 결과는 `location`·`startDate` 를 요구한다. 뉴스적 "사건"에 붙이면 규격 위반으로 GSC 오류가 난다. `NewsArticle` 의 `datePublished`(최초 보도) / `dateModified`(최신 갱신)가 실시간성을 표현하는 정확한 수단이다.

### 5.3 토론 상세 — 채팅 기반 진행 경과

**본문**: H1 스레드 제목 → 찬반 현황(`agreeCount`/`disagreeCount`, 총 참여 수) → **진행 경과 요약** → 대표 발언(찬성/반대 각각) → 전체 채팅 내역 → 상위 이슈 링크.

**JSON-LD**

```
DiscussionForumPosting
  @id                    {SITE_URL}/issue/8-.../debate/61-...#thread
  headline               스레드 제목
  datePublished / dateModified
  interactionStatistic   InteractionCounter × N (찬성 수 / 반대 수 / 발언 수)
  comment                Comment[] (작성자는 닉네임, 본문, 시각)
  isPartOf               이슈 Article
BreadcrumbList
```

`DiscussionForumPosting` 은 Google 이 "Discussions and forums" 리치 결과를 위해 지정한 타입이다. 레딧·커뮤니티가 검색 상위를 먹는 그 자리다. 우리 콘텐츠 형태와 정확히 일치한다.

> **두 개의 선결 조건**
> 1. **채팅 조회 API 가 401 이다.** 서버에서 못 읽으면 본문이 없다 → §7-1
> 2. **개인정보 문제.** 채팅에는 닉네임·소속 커뮤니티·의견이 붙는다. 이걸 검색엔진에 공개 색인시키려면 이용약관·개인정보처리방침에 "게시물의 공개 및 검색엔진 색인" 조항이 있어야 하고, 사용자가 사전에 인지해야 한다 → §7-2

### 5.4 AI 뉴스 기사 (예약)

**JSON-LD**: `NewsArticle` + `BreadcrumbList` + `about` → 관련 이슈.

> **여기에는 실질적 리스크가 있고, 그냥 넘기지 않는다.** Google 스팸 정책(2024-03 도입, developers.google.com/search/docs/essentials/spam-policies)의 **"scaled content abuse"** 는 AI 생성 자체를 벌하지 않는다. 대상은 *"검색 순위를 조작할 목적으로 대량 생성되고 사용자에게 실질적 가치가 없는 페이지"* 이고, **판정 기준은 생성 방식이 아니라 페이지당 가치**다.
>
> **그리고 도메인 단위로 작동한다.** 한 도메인이 판정을 받으면 이슈·토론 페이지까지 같이 죽는다. v0.9.0 의 성과 전체가 뉴스 하나에 걸린다.
>
> 발행 게이트 — 하나라도 못 지키면 **`/news/` 를 Disallow 로 두고 시작한다.** 색인은 나중에 켤 수 있지만 판정은 회복에 수개월이 걸린다.
>
> - 발행 전 **사람의 편집 검수**. 무검수 자동 발행 금지
> - 본문과 `NewsArticle` 에 **AI 생성 사실 명시**(`creditText` + 화면 상단 라벨)
> - `publisher` 는 조직(토론철). **가상의 기자 이름을 만들지 않는다**
> - **발행량을 인간 검수 역량에 연동**한다. 검수 없이 늘리지 않는다
> - 기사가 **토론철 1차 데이터**(찬반 수치·커뮤니티 분포·여론 변화)를 근거로 삼는다. 외부 기사 리라이트는 정확히 최대 피해 패턴이다
> - **인터넷신문 등록 여부 확인.** 등록 전에는 Disallow 유지가 안전하다
>
> **우리에게는 상쇄 카드가 있다.** 탐지 신호 중 "원본 조사 부재"를 정면으로 지우는 자산 — 찬반 수치·참여 커뮤니티 분포·발언 수·시간에 따른 여론 변화는 **다른 어디에도 없는 1차 데이터**다. 동시에 Cite Sources·Statistics 는 GEO 실측 상위 3위 기법이다. 두 목적이 같은 방향을 가리키는 드문 경우다.
>
> **발행 속도의 구체 수치는 규칙화하지 않는다.** "일 10건 이상이면 자동 생성 신호" 류의 주장이 돌지만 Google 공식 문서에 수치는 없다(블로그 단일 출처 ⚠). 다만 일 1,000건 계획이 나온다면 그 주장의 100배이고, **자릿수가 다르면 출처의 강도와 무관하게 논쟁의 여지가 없다.**
>
> 상세 절차·체크리스트·`generateSitemaps` 코드 패턴은 **`.claude/skills/seo-geo/references/ai-news.md`** 에 있다. 이건 v0.9.0 이 아니라 뉴스 기능 착수 시점의 결정 사항이다.

### 5.5 전역 (`layout.tsx`)

```
metadataBase        new URL(SITE_URL)
title.template      "%s | 토론철"
title.default       "토론철 — 이슈와 토론"
robots              index/follow, googleBot max-snippet:-1, max-image-preview:large
openGraph           locale ko_KR, siteName 토론철, type website
twitter             summary_large_image
JSON-LD             Organization { name, alternateName["Toronchul"], url, logo, sameAs[] }
```

> `WebSite` + `potentialAction: SearchAction`(사이트링크 검색창)은 **넣지 않는다.** 사이트 내 검색 URL(`/search?q=`)이 실제로 없다. 없는 기능을 마크업하면 규격 위반이다. `sameAs` 도 마찬가지 — 앱스토어·구글플레이·SNS 의 실제 URL 을 확인한 것만 넣는다. (§7-4)

---

## 6. 인프라

### 6.1 `src/lib/seo.ts` — 단일 출처

```ts
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://toronchul.app";
buildMetadata(input): Metadata            // canonical·OG 는 상대 경로
buildAlternates(path): Metadata["alternates"]  // hreflang ko/en/x-default
issueJsonLd(...) / debateJsonLd(...) / newsJsonLd(...) / breadcrumbJsonLd(...)
organizationJsonLd()
```

| 위치 | URL 형식 | 이유 |
|---|---|---|
| `Metadata.alternates.canonical`, `openGraph.url`, `openGraph.images[].url` | **상대** | `metadataBase` 가 절대화 |
| JSON-LD 의 `@id`·`url`·`mainEntityOfPage`·`BreadcrumbList.item` | **절대** | 스펙 필수 |
| `sitemap.xml` 의 `<loc>`, `robots.txt` 의 `Sitemap:` | **절대** | 스펙 필수 |

`NEXT_PUBLIC_SITE_URL` 을 `.env.example` 에 추가한다. **도메인 문자열 하드코딩 금지, `const SITE_URL` 로컬 선언 금지.**

### 6.2 `src/app/sitemap.ts`

단일 파일. 50개 URL 규모라 분할·페이지네이션·OOM 대책은 불필요하다(§3.3).

> **단, AI 뉴스가 생기면 자릿수가 바뀐다.** 일 1,000건 × 1년 = 36.5만 URL → 뉴스만 8개 샤드다. 그때 쓸 시그니처를 지금 고정해 둔다 — **Next.js 16 에서 `generateSitemaps` 의 `id` 가 Promise 로 전달된다.** 이전 버전의 평문 값 시그니처를 그대로 쓰면 깨진다.
>
> ```ts
> export default async function sitemap(props: { id: Promise<number> }) {
>   const id = await props.id;   // ← Next.js 16. number 로 받으면 깨진다
>   const start = id * 50_000;   // Google 한도: 사이트맵 파일당 50,000 URL
> }
> ```
>
> **v0.9.0 에는 넣지 않는다.** 50개 URL 에 샤딩은 순수 비용이다. `references/ai-news.md` §3.

| URL | 소스 | `lastModified` |
|---|---|---|
| `/`, `/map` | 정적 | 고정 상수 `STATIC_LAST_MODIFIED` |
| `/issue/{id}-{slug}` | `GET /api/v1/users/home` | `wiki.updatedAt` ?? 최신 스레드 활동 ?? `issue.createdAt` |
| `.../debate/{id}-{slug}` | 이슈별 `GET /api/v2/room` | 스레드 최종 활동 시각 |

> **`lastModified` 에 `new Date()` 절대 금지.** 빌드할 때마다 전 URL 이 "방금 수정됨"으로 나가면 Google 이 lastmod 신호 자체를 신뢰하지 않게 되고, 회복에 수 주~수 개월이 걸린다.
>
> **문제 있음, 그리고 그대로 넘기지 않는다** — 현재 API 가 주는 시각 필드는 `issue.createdAt` 뿐이다. `IssueBriefResponse` 에 `updatedAt` 이 없고, `ThreadSummary` 에는 시각 필드가 아예 없다. 스레드의 최종 활동 시각을 알 수 없다. → §7-3 백엔드 요청. 그전까지는 `createdAt` 으로 정직하게 채운다(부정확한 최신 날짜보다 정확한 과거 날짜가 낫다).

### 6.3 `src/app/robots.ts`

```
*               allow /, disallow: /api/, /proxy/, /relay/, /oauth/, /login,
                                   /settings, /profile, /onboarding, /*?*
Googlebot       allow /
Bingbot         allow /, crawlDelay 1
Yeti            allow /, crawlDelay 1        ← [P0] 누락 시 Naver 전체 차단 위험
Daumoa          allow /
AI 검색·브라우징 봇 허용 : OAI-SearchBot, ChatGPT-User, Claude-SearchBot, Claude-User,
                          PerplexityBot, Google-Extended, Applebot-Extended,
                          Meta-WebIndexer, DuckAssistBot, YouBot, MistralAI-User
AI 학습 봇 차단        : GPTBot, ClaudeBot, CCBot, Bytespider, meta-externalagent,
                          DeepSeekBot, cohere-ai, Diffbot, AI2Bot
소셜 봇 허용           : facebot, Twitterbot, kakaotalk-scrap
Sitemap: {SITE_URL}/sitemap.xml
```

> **AI 봇 3계층은 GEO 레버가 아니라 라이선스 결정이다.** (2026-08-21 리서치로 정정)
>
> 벤더별로 retrieval 봇과 training 봇이 따로 제어되는 것은 사실이다(`GPTBot`↔`OAI-SearchBot`, `ClaudeBot`↔`Claude-SearchBot`). **그러나 학습 봇을 막는다고 인용이 유지되지도, 열어둔다고 인용이 늘지도 않는다.**
>
> - `GPTBot` 을 차단해도 콘텐츠가 Common Crawl·Bing 인덱스를 통해 도달해 ChatGPT 가 인용한다
> - AI Mode 피인용 도메인의 **51.9%가 AI 크롤러를 차단 중**이다(전체 표본 15% 대비). 차단 도메인이 오히려 **3.5배 더 인용된다** — Google 의 그라운딩은 AI 전용 크롤러가 아니라 자기 인덱스를 읽기 때문
> - `Google-Extended`·`Applebot-Extended` 는 **HTTP 요청을 하지 않는 robots.txt 전용 opt-out 토큰**이다. 차단해도 검색 순위·Siri 노출에 영향이 없다
> - **`ChatGPT-User` 같은 user-triggered fetcher 는 robots.txt 를 지키지 않는다**(OpenAI·Google 모두 문서화). Anthropic 의 `Claude-User` 만 예외적으로 준수한다 → robots.txt 를 접근 통제로 착각하지 말 것
>
> **따라서 학습 봇 차단/허용은 저작권·사업 결정이다.** 토론철이 정식 인터넷신문사 등록을 목표로 한다면 법무 판단 사항이고(뉴스 발행사의 AI 크롤러 차단률은 56.4%로 일반 사이트의 5배다), SEO 로서는 어느 쪽도 권고하지 않는다. **결정된 정책을 robots.txt 에 정확히 옮기는 것까지가 우리 일이다.** → §7-9
>
> **`/relay/` 차단은 필수다.** PostHog 리버스 프록시 경로다(v0.8.0). 크롤러가 수집 엔드포인트를 두드리면 분석 데이터가 오염된다.
>
> **`kakaotalk-scrap` 허용은 우리에게 특히 중요하다.** 국내 유입의 상당수가 카카오톡 공유다. 막으면 링크 미리보기가 죽는다.

### 6.4 RSC 전환 — 실제 작업량의 대부분

패턴은 **서버 셸 + 클라이언트 아일랜드** 다.

```
page.tsx  (서버)  generateMetadata + JSON-LD + 위키 본문 + 스레드 목록 + 타임라인
  └─ <ChatIsland />  ("use client")  실시간 채팅·투표·소켓
```

- 크롤러는 서버 셸의 완성된 HTML 을 읽는다
- 사용자는 그 위에 하이드레이션된 인터랙션을 얻는다
- **핵심 콘텐츠가 클라이언트 아일랜드 안에 있으면 안 된다.** 그러면 다시 30자로 돌아간다
- 데이터 재검증: `apiFetch` 는 나머지 옵션을 `fetch` 로 그대로 넘기므로 `next: { revalidate: N }` 이 통한다. 위키·이슈는 길게(예: 1시간), 스레드 목록은 짧게

---

## 7. 결정 / 요청이 필요한 것

| # | 항목 | 대상 | 막히는 것 |
|---|---|---|---|
| **7-1** | **채팅 조회의 공개 읽기 경로** — `GET /api/v1/chat/rooms/{roomId}/messages` 가 401. 익명 읽기 허용 또는 별도 공개 엔드포인트 | 백엔드 | **토론 상세 페이지 본문 전체.** v0.9.0 최대 블로커 |
| **7-2** | 채팅 내역의 검색엔진 공개 색인에 대한 약관·개인정보처리방침 근거와 사용자 고지 | 법무/기획 | 토론 상세 페이지 출시 |
| **7-3** | `IssueBriefResponse.updatedAt`, `ThreadSummary` 의 최종 활동 시각 필드 | 백엔드 | 정확한 `lastmod` |
| **7-4** | Organization `sameAs` 에 넣을 실제 URL — 앱스토어·구글플레이·SNS·회사 사이트 | 기획 | Organization 스키마 완성도 (추측 금지) |
| **7-5** | 이슈 `about.sameAs` 용 외부 레퍼런스(위키백과 등) URL 을 어떻게 채울지 — 관리자 입력 필드 or 수동 매핑 | 기획/백엔드 | GEO 엔티티 연결 |
| **7-6** | "사건" 엔티티의 데이터 모델·API | 백엔드 | 사건 상세 페이지 (v0.9.0 은 자리만 예약) |
| **7-7** | GSC·Naver 서치어드바이저·Bing 계정 소유권과 verification 토큰 | 운영 | 색인 요청·측정 |
| **7-8** | 자동 번역의 실행 방식 — 빌드 타임 사전 번역 / 요청 시 번역+캐시 / 백엔드가 번역본 저장 | 기획 | `/en` 실제 구현 (v0.9.0 은 URL 자리·hreflang 만) |
| **7-9** | **AI 학습 크롤러 차단/허용 방침** — GPTBot·ClaudeBot·CCBot 등 9종. **SEO 결정이 아니라 저작권·사업 결정이다**(§6.3). 인터넷신문 등록 계획과 연동 | 법무/사업 | `robots.ts` 의 해당 블록. 검색 인용에는 영향 없으므로 v0.9.0 을 막지는 않는다 |
| **7-10** | **Naver 메타 길이 기준 확인** — title 40자 / description 80자 주장이 블로그 단일 출처다 ⚠. Search Advisor 공식 가이드로 재확인 | 운영 | 메타 문구 규칙 확정 (그전까지는 권고로만) |
| **7-11** | **RSS/Atom 피드** — Naver 색인은 sitemap 과 피드를 **둘 다** 요구한다. 토론철에는 피드가 없다 | 개발 | Naver 색인 완결. AI 뉴스 시 특히 필요 |

---

## 8. 범위

### 8.1 넣는 것

| # | 항목 |
|---|---|
| 1 | URL 스킴 확정 + `src/lib/slug.ts` 재작성 (10진수 id, `{id}-{slug}` 융합, NFC, 길이 상한) |
| 2 | 잘못된/누락된 slug → 정식형 301 |
| 3 | 라우트 재배치 (`/issue/[issueSlug]`, `.../debate/[threadSlug]`) |
| 4 | `src/lib/seo.ts` — SITE_URL·`buildMetadata`·JSON-LD 빌더 |
| 5 | `layout.tsx` 전역 메타 + Organization JSON-LD |
| 6 | 이슈 상세 **RSC 전환** + 위키 본문 렌더 + `generateMetadata` + JSON-LD |
| 7 | 토론 상세 **RSC 전환** + `generateMetadata` + JSON-LD (본문은 7-1 해결 시) |
| 8 | `robots.ts` (AI 봇 3계층 포함) |
| 9 | `sitemap.ts` (이슈 + 토론) |
| 10 | hreflang `alternates.languages` 구조 (ko 만 채움, en 자리 예약) |
| 11 | OG 이미지 (정적 1종 + 엔티티별 동적은 후속) |
| 12 | `.claude/skills/seo-geo/` — `SKILL.md` + `references/{geo-evidence,crawler-policy,schema-map,content-patterns,ai-news}.md` + `scripts/audit.sh` |

### 8.2 넣지 않는 것

| 항목 | 이유 |
|---|---|
| `/en` 실제 번역 | 번역 파이프라인 미정(7-8). v0.9.0 은 **URL 자리와 hreflang 구조만** |
| 사건 상세 페이지 | 백엔드 없음(3.4) |
| AI 뉴스 기사 페이지 | 백엔드 없음 + 정책 선결(5.4) |
| IndexNow | 갱신 빈도가 낮아 실익이 적다. Bing 색인이 안 붙으면 재검토 |
| WAF · 크롤 버짓 대응 | 규모·인프라가 다르다(3.3) |
| 사이트맵 분할 · Naver 전용 사이트맵 | URL 50개 |
| Thin Content 경고 배너 | 우리 문제는 표본이 아니라 본문 부재(3.3) |
| 사이트 내 검색 + `SearchAction` | 기능 자체가 없다 |

---

## 9. 검증 체크리스트

### 9.1 URL
- [ ] `/issue/8-도널드-트럼프-2기-행정부` 200
- [ ] `/issue/8` → 301 → 정식형
- [ ] `/issue/8-틀린슬러그` → 301 → 정식형
- [ ] `/issue/abc-...` → 404
- [ ] `/oauth/callback/kakao` 정상 (로케일 미들웨어 미개입) — **카카오 로그인 실기기 확인**
- [ ] URL 문자열을 직접 조립한 코드 0건 (`grep -rn '"/issue/' src/`)

### 9.2 크롤러 관점 실측 — v0.9.0 의 합격 기준
```bash
curl -A "Mozilla/5.0 (compatible; Googlebot/2.1; +http://www.google.com/bot.html)" \
     https://toronchul.app/issue/8-... | <태그 제거> | wc -m
```
- [ ] 본문 텍스트 **30자 → 500자 이상** (위키 게시 시 2,000자 이상)
- [ ] `<title>` 이 이슈 제목 포함, 페이지마다 고유
- [ ] `description` 50자 이상, 155자 이하, `\n` 없음
- [ ] `<link rel="canonical">` 절대 URL, 정식형과 일치
- [ ] JSON-LD 존재 · 화면 콘텐츠와 일치

### 9.3 인프라
- [ ] `bash .claude/skills/seo-geo/scripts/audit.sh /issue/8-...` 전 항목 통과
- [ ] `/robots.txt` 200, `Sitemap:` 절대 URL
- [ ] **`robots.txt` 에 `Yeti` 가 이름으로 있다** [P0] — 누락 시 Naver 전체 차단
- [ ] **`public/naver*.html` 이 어떤 Disallow 에도 걸리지 않고, sitemap 에도 없다**
- [ ] `/sitemap.xml` 200, 전 이슈·전 스레드 포함
- [ ] `lastmod` 에 빌드 시각 없음 (`grep -n 'new Date()' src/app/sitemap.ts` → 0건)
- [ ] noindex 페이지가 사이트맵에 없음 (역도 성립)
- [ ] canonical ↔ OG url ↔ sitemap `<loc>` 3자 일치

### 9.4 검증 도구
- [ ] Rich Results Test — Article / DiscussionForumPosting / BreadcrumbList 오류 0
- [ ] Schema.org Validator 오류 0
- [ ] GSC · Naver 서치어드바이저 · Bing 사이트맵 제출 (**Naver 는 RSS/Atom 도 필요** → §7-11)
- [ ] 봇 UA ↔ 브라우저 UA 응답 차이 10% 이내 (클로킹 아님) — `audit.sh` §2
- [ ] `npm run build` · `npm run lint` · `npx tsc --noEmit` 통과

### 9.5 불문율 정합
- [ ] `apiFetch` 경유 (SSR 포함), 직접 `fetch(` 0건
- [ ] `any` · `@ts-ignore` · `!` 0건
- [ ] Swagger/실 curl 로 확인한 필드만 사용 — 사건·뉴스 DTO 를 지어내지 않았음
- [ ] TDS 컴포넌트가 `@/lib/seo` 를 import 하지 않음 (leaf 유지)
- [ ] hex/rgb 0건

---

## 10. 단계

| 단계 | 내용 | 선결 |
|---|---|---|
| **1** | URL 스킴 + `slug.ts` + 301 + 라우트 재배치 | — |
| **2** | `seo.ts` + `layout.tsx` + `robots.ts` + `sitemap.ts` | 1 |
| **3** | 이슈 상세 RSC 전환 + 위키 렌더 + JSON-LD | 1, 2 |
| **4** | 토론 상세 RSC 전환 + JSON-LD | 7-1, 7-2 |
| **5** | GSC/Naver/Bing 제출 → 2~4주 색인 관찰 | 1~4 |
| **6** | 사건 · AI 뉴스 · `/en` | 7-6, 7-8, 5.4 정책 |

---

## 11. 성공 지표

| 지표 | 현재 | v0.9.0 목표 |
|---|---|---|
| 색인된 페이지 수 | **0** | 이슈 17 + 토론 30 중 70%+ |
| 크롤러가 읽는 본문 텍스트 | **30자** | 500자+ (위키 게시 시 2,000자+) |
| `site:toronchul.app` 결과 | 사실상 없음 | 전 이슈·토론 노출 |
| 구조화 데이터 오류 (GSC) | — | 0건 |
| AI 검색 인용 | 0 | 이슈명 질의 시 토론철 인용 확인 (정성) |
